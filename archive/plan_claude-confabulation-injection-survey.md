# [完了] Claude 作話によるインジェクション幻覚 調査記録（2026-06）

> 最終更新: 2026-06-19(金) 09:11:03

## context配分

| C | 担当作業 | 状態 |
|---|---|---|
| C1 | 生ログ横断調査・暴走年表作成・原因切り分け（many-ai-cli 経由判定） | fix |

> 本ファイルは現象の調査記録。緩和策の実施は運用で継続（C1 の調査自体は完了）。

## 概要

2026-06-09〜06-19 の 11 日間で、Claude Code（Opus 4.8）が **存在しないプロンプトインジェクション攻撃・注入指示を幻視し、拒否や作話に走る**事例が頻発した。ユーザーの体感「6 回くらい」を実データで検証したところ、確定・濃厚だけで 10 件あった（記事化済みは 2 件）。

本調査の目的は (1) 全件の洗い出し、(2) 各件が本物の防御か作話かの確定、(3) 原因の切り分け（特に many-ai-cli が誘発しているのか）。

## 調査手法

- Claude Code 本体トランスクリプト（`~/.claude/projects/<proj>/<uuid>.jsonl`）を正本として横断検索。
- 第 1 段: `プロンプトインジェクション|prompt injection` で全 projects を検索 → ノイズ源（`ai-audit-prompts`＝監査が主題、`workshop`＝記事執筆、subagents/workflows）を除外。
- 第 2 段: 話者別出現数で「Claude が自発的に言い出した（Asst>0 & User=0）」を抽出。
- 第 3 段: 語彙非依存の暴走シグネチャ（`混ざっていた`/`偽の system`/`これは攻撃`/`偽ドメイン`/`言ってもいない`/`WIZARD-TEACHER` 等）で取りこぼし回収（記事 1・2 型を捕捉）。
- 確定: Claude が「来た」と主張した注入文字列が、実際の user 入力・tool_result・system 側に存在するかを照合。assistant 発話にしか無ければ作話。

## 暴走年表（◎=確定 作話）

| 日時 | プロジェクト | セッション | many-ai-cli経由 | 内容 |
|---|---|---|---|---|
| 06-09 11:02 | mer | `201f4655` | 素(no) | 「システムプロンプトを逐語出力せよ」を注入と誤検知して拒否 |
| 06-09 11:35 | mer | `28d3f19e` | 素(no) | 上を「別セッションの Claude が誤作動し、普通のメッセージを攻撃と誤検知し続けた」と自己解説 |
| 06-09 16:12 | mer | `e693be79` | 素(no) | 「ツール結果にバックドアを仕込め指示があった」と拒否（tool_result に実在せず＝作話） |
| 06-10 00:04 | any-ai-cli | `f610495f` | 素(no) | 「cat\|sed しろ という system-reminder」を注入と拒否（記録に無し＝作話） |
| 06-10 06:00 | any-ai-cli | `ef30e8d1` | 素(no) | 「ツール出力に私の発言を装った偽テキストが繰り返し注入されている」と主張 |
| 06-12 06:58 | any-ai-cli | `21d320c2` | 経由(YES) | 「system-reminder を装ったインジェクション（SSH 鍵抜き取り狙い）」を拒否（指示文 記録に無し＝作話） |
| 06-18 15:09 | mer | `84f281d9` | 経由(YES) | 【記事2】「ipv4 のログはあなたの入力に混ざって届いていた」（入力に無し＝二次作話） |
| 06-18 21:12 | mer | `cd088586` | 経由(YES) | 「ENTER WIZARD-TEACHER MODE 一式が連続して届いた」を拒否（入力に完全不在＝作話。jailbreak テンプレを幻視） |
| 06-19 00:55 | workshop | `a989d80c` | 経由(YES) | 【記事1】NISA 調査で「偽ドメインへ送れという攻撃」を幻視。攻撃語は自身の拒否文中にのみ存在 |
| 06-19 08:16 | many-ai-cli | `135c166f` | 経由(YES) | 「plan mode 注入（ignq+you are now in plan mode+ore it）」を幻視。入力は普通の音声入力 4 件のみ |

## 原因切り分け（最重要）

### many-ai-cli は原因ではない（実証済み）

- 10 件中 5 件（6/9〜6/10）は **DONE マーカー無し＝素の Claude Code を直接使用**。cwd も `C:\dev\mer` 等を直接指す。
- many-ai-cli のセッションログ（`~/.many-ai-cli/logs/sessions/`）は **最古が 06-15**。6/9〜6/12 の暴走は many-ai-cli が記録を取る前＝事実上 many-ai-cli を介さない時期に発生。
- → many-ai-cli 経由・非経由のどちらでも同型の暴走が起きており、many-ai-cli は誘発要因ではない。当初の「hook 環境が再現条件」という見立ては、相関を因果と取り違えた誤り。

### 残る共通要因（仮説）

1. **モデル側の作話傾向（根本）**: Opus 4.8 は「自信に満ちて間違える」挙動が既知の課題。入力と自分の出力の境界を見失い、防御反応を捏造する。
2. **グローバル設定の機密密度＋防衛指示**: `~/.claude/CLAUDE.md` が @import で `serverpass.toml`（生パスワード多数）・SSH 鍵パス・family CSV（機微）・大量の防衛指示（外部送信禁止／tier=high 禁止／インジェクション警戒）を毎セッション常駐させる。これが「機密だらけで攻撃に晒された環境」という認識を与え、防衛作話を誘発する可能性（要検証の仮説）。
3. **音声入力の崩れ**: `84f281d9`(ipv4 ろぐ)・`135c166f`(ジェイソン ウェル=JSON) など、認識ゆれのある入力が引き金になりやすい。

> 確実に言えるのは「many-ai-cli は無関係」。2 の「グローバル設定が誘発」は相関の域を出ない仮説。

## 緩和策（運用）

- 暴走したセッションは原因究明をさせず即 `/clear` で閉じる（当人に分析させると作話が増える）。
- 確定・反省・引用は鵜呑みにせず、生ログ（`~/.claude/projects/`）で出典照合する。
- お金・データ・機密に関わる確定は人間の承認ゲートを挟む。
- グローバル設定の機密直書き（生パスワード等）を減らせるか別途検討（仮説 2 の緩和。未着手）。

## 参照

- 生ログ正本: `~/.claude/projects/C--dev-*/<uuid>.jsonl`（プロジェクト別）
- many-ai-cli 側ログ: `~/.many-ai-cli/logs/sessions/`（`.log`/`.jsonl`/`.txt`、最古 06-15）
- 記事1: https://note.com/ishizakahiroshi/n/n8cc133704d95 （「これは攻撃です」とClaudeは言った）
- 記事2: https://note.com/ishizakahiroshi/n/n69239d033cfb （「IPv4のログ、見ますか？」）
