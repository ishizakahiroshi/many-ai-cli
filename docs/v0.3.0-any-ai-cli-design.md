# any-ai-cli 設計書 v0.3.0

> 最終更新: 2026-06-11(木) 22:15:59
> 本書は v0.2.0 設計書（`docs/v0.2.0-any-ai-cli-design.md`）をベースに、v0.3.0 で追加・変更された機能を現行ソース・README・CHANGELOG と突き合わせて反映した詳細メモ。公開仕様は README、実装仕様はソースコードを正本とする。

## 0. v0.2.0 からの主な差分（サマリ）

v0.3.0 は v0.2.0 系（v0.2.0 / v0.2.2）からの minor release。テーマは「単発の承認ビュー＋作業面」から「**セッション履歴を残し、複数 Hub を遠隔運用できる常駐基盤**」への拡張。

主な追加:

- **Workbench タブ + SQLite セッション履歴**: PTY セッション・タイムライン・チャット・承認・添付を `~/.any-ai-cli/logs/any-ai-cli.db`（modernc.org/sqlite, pure-Go）へ蓄積し、横断検索・要約・エクスポート・タスクボード・プロンプトテンプレート・診断などを 1 タブに集約。
- **承認（Approval）専用タブ**: 全セッションの承認待ちをカード一覧で一括処理（モバイル動線強化）。
- **PWA + opt-in Web Push**: manifest / service worker / アイコンを同梱し、承認待ち・タスク完了を Web Push 通知（VAPID 鍵はサーバ自動生成。通知に Hub token を含めない）。
- **統合 Windows ランチャー**: 旧 `any-ai-cli-wsl.exe` を廃止し、`any-ai-cli-launcher.exe` が WSL / SSH serve / SSH tunnel を 1 プロファイルファイルで一元管理。常駐リモート Hub への再接続に対応。
- **VPS / Docker デプロイ資産**: GHCR への自動 publish、loopback-only な per-user compose サンプル、リソース制限、health check、冪等な `aac-update.sh`。
- **Files / Git 操作の拡張**: フォルダ新規作成・空ファイル作成・base-mtime 競合検出付き保存・空フォルダ削除（Files）、`git fetch` / `git pull --ff-only`（Git）。
- **音声（Whisper）/ outbound 通知（ntfy・webhook）/ トークンコストバー** の設定追加。

CHANGELOG 上のリリース日は v0.3.0 = 2026-06-05。

## 1. v0.3.0 の位置づけ

v0.2.0 が「Hub UI を承認ビューから複数セッション運用の作業面へ拡張」したのに対し、v0.3.0 は次の 2 軸で常駐運用を強化する。

1. **永続化軸**: セッションを SQLite に残し、再起動・横断検索・エクスポート・要約・利用量集計を可能にする（Workbench）。
2. **遠隔運用軸**: WSL / VPS（SSH serve / tunnel）/ Docker（GHCR + compose）を統合ランチャーと PWA/Web Push で結び、ローカル端末から常駐 Hub を安全に操作する。

セキュリティモデル（`127.0.0.1` bind 固定・token 必須・外部公開禁止）は変更しない。遠隔運用はすべて SSH local forward または loopback publish の上に成り立つ。

## 2. 公開スコープ

公開 README 上の wrap 対象は Claude Code / Codex CLI / GitHub Copilot CLI / Cursor Agent CLI。Gemini CLI は対象外。OpenCode は UI / config / usage-links に痕跡が残るが、`/api/spawn` のバリデーションは `claude` / `codex` / `copilot` / `cursor-agent` の 4 provider のみを許可し、OpenCode は v0.3.0 でも非公開・spawn 非対応。

Ollama は provider ではなく route / model backend。Claude Code / Codex CLI の子プロセスに環境変数を注入し、既存 provider CLI を Ollama 経由で使う。

検証状況:

- 実機検証済み: Windows、WSL（統合ランチャー `wsl` プロファイル経由）、VPS（Docker / SSH tunnel 経由の常駐 Hub）
- native Linux / macOS: build 対象だが十分な実機検証は未完了
- ビルド要件: Go 1.25（Docker / GoReleaser とも）

## 2-1. Runtime / Environment Identity

Hub UI は `GET /api/info` を single source として実行環境を表示する。`runtime_mode` / `runtime_label` / `ssh` / `host_ip` は後方互換のため残し、ブラウザタブ・favicon・ヘッダー badge の瞬間識別には `env_*` を使う（v0.2.0 から変更なし）。

`env_kind` の標準値:

| env_kind | 表示 | 用途 | favicon |
|---|---|---|---|
| `local` | Local | ローカル OS 上で直接起動した Hub | 緑 + `L` |
| `wsl` | WSL | WSL 内、または Windows launcher 経由の WSL Hub | 青 + `W` |
| `vps` | VPS | SSH 経由で起動した VPS 上の Hub | オレンジ + `V` |
| `vps-tunnel` | VPS Tunnel | SSH local forwarding 経由で常駐 VPS Hub を操作 | 赤 + `T` |

判定優先順は `ANY_AI_CLI_ENV_KIND` → `config.yaml:hub.env_kind` → `/api/net-hint` → runtime / SSH 環境変数 → `local`。不正な `env_kind` は `local` へフォールバックする。`ANY_AI_CLI_HOST_LABEL` と `/api/net-hint.host_label` は `env_host_label` として `/api/info` に返し、VPS / tunnel の識別補助に使う。

`GET /api/info` の環境識別フィールドは v0.2.0 と同一（`env_kind` / `env_label` / `env_short` / `env_color` / `env_title` / `env_host_label`）。

## 3. Hub UI のタブ構成

v0.3.0 のメイン領域は単一の unified tab bar（`#unified-tab-bar`）に統合された 8 タブ。

| 順序 | data-tab | タブ | 役割 | 実装 |
|---|---|---|---|---|
| 1 | `terminal` | Terminal | xterm.js のライブ PTY 表示（デフォルト） | `web/src/app/terminal.ts` |
| 2 | `multi` | Multi | 複数セッションのグリッド表示（レイアウトバッジ付き） | `web/src/app/multi-pane.ts` |
| 3 | `chat` | Chat | PTY stream から抽出した会話履歴（未読バッジ付き） | `web/src/app/chat-history.ts` |
| 4 | `split` | Split | Terminal と Chat の横並び | `web/src/app.ts` |
| 5 | `files` | Files | project file browser / preview（遅延ロード） | `web/src/app/files-view.ts` |
| 6 | `git` | Git | commit log / diff / commit all / fetch / pull（遅延ロード） | `web/src/app/git-view.ts` |
| 7 | `workbench` | Workbench | セッション履歴・タスク・テンプレート等の作業ハブ（**v0.3.0 追加**） | `web/src/app/workbench.ts` |
| 8 | `approval` | 承認 | 全セッション承認待ちカード一覧（未処理件数バッジ付き、**v0.3.0 追加**） | `web/src/app/approval-queue-tab.ts` |

Files / Git は `lazy` クラスで初回クリック時に backend API を呼ぶ。Workbench は `workbench-opened` カスタムイベントで初期化する。`#main-tab-bar` は Files タブ内の複数タブ管理（FilesTabManager）用の隠し DOM として残置。

## 4. Chat / Split

v0.2.0 から仕様は基本変更なし。Chat はセッションごとに `Message[]` を in-memory で保持し、データ源は PTY stream と UI 操作イベント（user input / AI output / approval / attach）。ターン境界は Hub から挿入される OSC marker `\x1b]47777;user-turn\x07`。

v0.3.0 では、ブラウザ再接続時の履歴 seed に加え、`GET /api/session-chat`（後述）で SQLite に保存された過去チャットを復元できる。Chat UI（吹き出し / provider icon・user avatar / copy / tool call 折りたたみ / raw PTY 確認 / filter / search / minimap）は v0.2.0 同等。

Split は terminal と chat を同時表示するモードで、bottom state を見て自動追従する。

## 5. Multi タブ

複数セッションを同時に見るためのグリッドビュー（v0.2.0 同等）。最大 18 ペイン想定、grid picker でレイアウト選択、active slot を sidebar / input / approval action-bar と同期、ResizeObserver で各ペインを fit / resize。入力欄・添付・approval action-bar は共有 UI のまま、フォーカス中ペインの session id に送信する。

## 6. Files タブ

Files は project file browser / preview。project group の Files 導線、セッションカード右クリック、`Ctrl+Shift+F` から開く。

Backend API:

| API | Method | 内容 |
|---|---|---|
| `/api/files-roots` | GET | session / project に基づく root candidates（git root + docs 候補） |
| `/api/files-list` | GET | root 以下の file tree flat list（最大 2000 件 / 深さ 8） |
| `/api/files-content` | GET | preview 用テキスト取得（1 MiB 上限） |
| `/api/files-asset` | GET | image / media preview asset |
| `/api/files-download` | GET | file download attachment |
| `/api/files-move` | POST | single / multi file move（preflight + rollback） |
| `/api/files-rename` | POST | 同一ディレクトリ内 basename rename |
| `/api/files-mkdir` | POST | **v0.3.0 追加。** 親 dir 直下に 1 段だけフォルダ作成（`MkdirAll` 不使用、既存は 409） |
| `/api/files-create` | POST | **v0.3.0 追加。** テキスト系 basename の空ファイル作成（`O_EXCL`、上書き・中間 dir 作成なし） |
| `/api/files-save` | POST | テキストファイルの上書き保存（base-mtime 競合検出付き、新規作成不可） |
| `/api/files-delete-dir` | POST | ディレクトリ削除（空ディレクトリ削除を想定。allowed root 自身・ファイル・symlink は拒否） |

### POST /api/files-save

リクエスト body（JSON）:

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `path` | string | 必須 | 書き込み対象ファイルの絶対パス |
| `content` | string | 必須 | 保存するテキスト内容 |
| `baseMtime` | string (RFC3339) | 省略可 | 競合検出用の基準 mtime。非ゼロ指定時はサーバ側 mtime と照合 |

レスポンス body（JSON）: `ok` / `path` / `size` / `mtime`（次回 `baseMtime`）/ `error` / `detail`。

エラーコード一覧:

| HTTP | `error` | 意味 |
|---|---|---|
| 400 | `bad_request` | path 未指定 / 相対パス / ディレクトリ指定 / JSON 不正 |
| 401 | — | token 不一致 |
| 403 | `forbidden` | allowed roots 外 / 非テキスト拡張子 |
| 404 | `not_found` | ファイルが存在しない（新規作成不可） |
| 409 | `conflict` | `baseMtime` と現在 mtime が不一致。レスポンスに現在 `mtime` を含める |
| 413 | `too_large` | content が 1 MiB 超 / request body が 2 MiB 超 |
| 500 | `internal_error` / `write_failed` | stat / write 失敗 |

制約:

- すべてのエンドポイントは `s.guard()`（token 検証 + メソッドチェック + POST 系の Host/Origin チェック）を通る
- traversal 防止のため allowed roots は cwd / git root に限定（`isPathUnderAllowedRoots` は `EvalSymlinks` 込みで判定）
- walk は depth / item count 上限あり、symlink は追跡しない
- preview text は許可拡張子とサイズ上限を持つ
- multi move は preflight 後 all-or-nothing。実行中失敗時は成功済み rename を逆順 rollback

Frontend: tree search / Markdown・code preview / image・media preview / context menu（copy path, open, reveal, move, rename）/ multi select + D&D / フォルダ・ファイル新規作成 / restored orphan tab の修復。

## 7. Git タブ

Git タブは repository inspection + 軽量操作 UI。branch badge click、session card context menu、`Ctrl+Shift+G` で開く。

Backend API:

| API | Method | 内容 |
|---|---|---|
| `/api/git-log` | GET | commit list。`ref`（`HEAD`/`<branch>`/`--all`）, `limit`（最大 1000）, `skip` 対応 |
| `/api/git-show` | GET | commit detail / file diff（diff は 256 KiB で truncation） |
| `/api/git-refs` | GET | local / remote / tag refs と HEAD（github.com は HTTPS URL も） |
| `/api/git-status` | GET | working tree status / branch / ahead-behind / upstream |
| `/api/git-commit-message` | POST | diff/stat ベースのヒューリスティック commit message 生成（LLM 不使用） |
| `/api/git-commit-all` | POST | `git add -A` + `git commit`（subject 200 / body 8192 文字上限） |
| `/api/git-fetch` | POST | **v0.3.0 追加。** `git fetch`（timeout 30 秒） |
| `/api/git-pull` | POST | **v0.3.0 追加。** `git pull --ff-only`（timeout 30 秒） |

`/api/git-pull` のエラー: 分岐あり → 409 `not_fast_forward`、未コミット変更あり → 409 `local_changes`、upstream 未設定 → 400 `no_upstream`。

`Commit all` は local commit のみで push は実行しない。modal は subject / body 編集、review gate、identity missing error を扱う。git command は timeout 付き（fetch / pull は 30 秒、その他は共通 5 秒）で実行し、セッション cwd を `resolveGitRoot(sid)` で検証する。

## 8. Approval

### Multi-question approval

`[ANY-AI-CLI]` 承認ブロック内に複数の番号付き質問がある場合、Hub は stacked choices として表示し、選択結果をまとめて PTY へ送る（v0.2.0 同等）。question ごとの選択状態保持 / progress・clear・keyboard navigation / Submit all で space-separated digit string 送信 / global numbering の補正 / false positive 時の banner dismiss。

### 承認キュータブ（v0.3.0 追加）

unified tab bar の「承認」タブ（`approval-queue-tab.ts`）は、全セッションの承認待ちをカード一覧で表示し、1 画面でまとめて処理できる。スマートフォンからの一括承認動線を強化したもので、未処理件数を tab バッジに表示する（`approval-queue.ts` がバッジ制御）。

### Go-side native approval detection

v0.2.0 で導入した Go 側 VT buffer による native approval detection を v0.3.0 で各 provider 向けに確定。`internal/hub/approval_detector.go` / `vt_buffer.go` が terminal output を正規化し、`approvalSourceGoVT = "go_vt"` として検出する。VT バッファは末尾 120 行を取得し、末尾 90 行を有効候補として検査する。

| provider | 検出方式 | kind |
|---|---|---|
| `claude` | 番号付きカーソル選択肢（Enter/ESC ヒント付きセレクタ含む） | `native` |
| `codex` | ショートカット `(y)/(p)/(n)/(esc)` 形式 | `native_codex_shortcut` |
| `copilot` | ショートカット `(y)/(n)/(!)/(#)/(?)/(esc)` 形式 | `native_copilot_shortcut` |
| `cursor-agent` | `(y)/(tab)/(shift+tab)/(esc or n)` 等の多様キー表記 | `native_cursor_agent_shortcut` |

検出時は WebSocket event `approval_detected` / `approval_cleared` を UI に送り、action-bar と状態表示へ反映する。

### Approval marker rule injection

承認ボタン機能が有効な場合、wrapped session の起動時に `[ANY-AI-CLI]` マーカー出力ルールを instruction file へ冪等注入する（v0.2.0 同等）。

| provider | 注入先 | 方式 |
|---|---|---|
| Claude | `~/.claude/CLAUDE.md` | `@~/.any-ai-cli/approval-rules.md` import 行 |
| Codex | `$CODEX_HOME/AGENTS.md`、未設定なら `~/.codex/AGENTS.md` | `<!-- any-ai-cli:approval-rules -->` 共有ブロック |
| GitHub Copilot | `<project instruction root>/AGENTS.md` | 共有ブロック |
| Cursor Agent | `<project instruction root>/AGENTS.md` | 共有ブロック |

`<project instruction root>` は git root を優先し、取れない場合は session cwd。共有ブロックは path 単位で冪等注入し、そのファイルを読む active wrap が 0 になった時点で any-ai-cli のブロックだけ削除する。

### Approval pattern profiles

承認 trigger phrase は `resources/approval-patterns/{claude,codex,copilot,cursor-agent,common}.md` を source とし、Hub 起動時に fetch / cache する。`<provider>.official.json`（読み取り専用）/ `<provider>.custom.json`（ユーザー編集）/ `<provider>.json`（active mirror）の 3 ファイル構成。

API: `GET /api/approval-patterns` / `GET|PUT|DELETE /api/approval-patterns/`。承認機能のオン/オフは `/api/approval/status` `/api/approval/enable` `/api/approval/disable` `/api/approval/dismiss`。remote sync で差分があれば `approval_patterns_updated` を broadcast し、UI が toast / reload する。`/approval-patterns/`（静的）は token 必須の専用ハンドラで配信する。

## 9. Model Picker / Ollama Route

Model picker は `web/src/app/spawn-panel.ts` と `/api/models` で実装される（v0.2.0 同等）。

| Group | provider | route | source |
|---|---|---|---|
| Anthropic | `claude` | `anthropic` | `resources/models/defaults.json` remote fetch + fallback |
| OpenAI | `codex` | `openai` | 同上 |
| GitHub Copilot | `copilot` | （空） | 同上 |
| Cursor Agent | `cursor-agent` | （空） | 同上 |
| Ollama Cloud | both | `ollama` | local daemon `/api/tags` の `remote_host` 付き aliases |
| Ollama Local | both | `ollama` | local daemon `/api/tags` + `config.yaml:local_models` |

`/api/models` は local daemon 結果を 60 秒 cache し、default model list は remote fetch 失敗時に hardcoded fallback を使う。デフォルト取得元 URL は `config.yaml:models_source` で上書きできる。

Spawn 時の route 判定: ①request の `route` 明示 → ②model が known Ollama IDs → ③model に `:cloud` を含む → ④Claude なら `anthropic` → ⑤Codex なら `openai` → ⑥Copilot は `""` → ⑦Cursor Agent は `""`。

Ollama route の env preset:

| provider | env |
|---|---|
| Claude | `ANTHROPIC_AUTH_TOKEN=ollama`, `ANTHROPIC_API_KEY=`, `ANTHROPIC_BASE_URL=http://localhost:11434` |
| Codex | `OPENAI_API_KEY=ollama`, `OPENAI_BASE_URL=http://localhost:11434/v1` |

Codex × Ollama では `--codex-oss` を wrapper へ渡す。Ollama route の model は `last_model` に保存しない。

## 10. Server-side user preferences

ユーザー機能設定は `config.yaml:user_prefs` に保存する。Frontend は初回 load 時に `GET /api/user-prefs` で server state を mirror し、localStorage 既存値を一度だけ server へ migrate する。以降は `setUserPref()` が localStorage cache と server PUT を同時に行う。

v0.2.0 からの代表項目（voice grace / auto-submit trigger / notification sound / approval auto-switch / quick commands / usage links / favorites / order 系 / cwd history / spawn defaults・last model / avatar・display name）に加え、v0.3.0 で以下が追加された。

| 追加キー | 型 | 用途 |
|---|---|---|
| `desktop_notifications.enabled` | bool | ページ内 Notification API による通知 |
| `push_notifications.enabled` | bool | Service Worker / Web Push 通知のオプトイン |
| `voice.input_disabled` | bool | 音声入力ボタンの無効化 |
| `display.theme` / `font_size` / `lang` / `locked_mode` | string | 表示設定（localStorage から server へ移行） |
| `token_statusbar.enabled` | *bool | トークン/コスト常時表示バー（既定 ON、三値 *bool） |
| `done_summary_notify.enabled` | bool | タスク完了サマリー通知 |

Custom notification sound は `~/.any-ai-cli/notify_sound_custom.bin` に別保存し、config には path / MIME だけを持つ（`/api/user-prefs/notify-sound-custom`）。Avatar file upload は `/api/user-prefs/avatar` と `/api/avatar` で扱う。

## 11. Workbench タブ + SQLite セッション履歴（v0.3.0 新規）

v0.3.0 の中心機能。PTY セッションを SQLite に蓄積し、Workbench タブから横断的に扱う。

### 11.1 SQLite session store

| 項目 | 値 |
|---|---|
| 実装 | `internal/sessionstore/store.go`（Hub からは `sessionstore.OpenForLogDir` で初期化） |
| ドライバ | `modernc.org/sqlite`（CGO 不要の pure-Go）+ 標準 `database/sql` |
| DB パス | `~/.any-ai-cli/logs/any-ai-cli.db`（WAL/SHM 同居、`any-ai-cli.db.reset-pending` でリセット予約） |
| PRAGMA | `auto_vacuum=INCREMENTAL` / `journal_mode=WAL` / `synchronous=NORMAL` / `foreign_keys=ON` / `busy_timeout=3000` / `SetMaxOpenConns(1)` |

テーブル:

| テーブル | 役割 |
|---|---|
| `sessions` | provider / cwd / branch / model / state / started_at / ended_at / title / tags_json / summary / archived |
| `events` | タイムラインイベント（type / ts / payload_json）。PTY 出力は `data_b64` を除去しテキスト先頭 2000 文字のみ保存 |
| `messages` | チャット履歴（role: user/ai/system, kind: text/attach, text, raw_text） |
| `approvals` | 承認イベント（sig / source / kind / question / context / options_json / state / selected_text） |
| `attachments` | 添付参照（path / filename / mime / size） |
| `messages_fts` | `messages` の FTS5 仮想テーブル（利用可能時のみ作成） |

書き込み・保守:

- PTY ホットパスからは `StoreEventAsync`（バッファ 4096 件のキュー）に積むのみで、`asyncWriter` goroutine が逐次コミット。キュー満杯時は無音破棄（全量は `.jsonl` に残る）
- `CloseStaleSessions`: Hub 起動直後に `ended_at IS NULL` の行を `disconnected` に一括補正
- `PruneOlderThan(cutoff)`: 終了済みセッションを時刻カットオフ削除（子テーブル 2000 件バッチ、60 秒の時間予算）
- `ResetHistory(preserveLiveIDs)`: 稼働中セッションを保護して全削除 → `VACUUM` → WAL truncate（VACUUM 失敗時はファイル再作成を次回起動に予約）

Workbench 用 JSON ファイル（DB とは別。`~/.any-ai-cli/workbench/` 配下）: `templates.json` / `prompt-palette.json` / `tasks.json` / `policy-profiles.json` / `workspace-state.json`。

### 11.2 Workbench / session-store API

| API | Method | 用途 |
|---|---|---|
| `/api/workbench/sessions` | GET | セッション一覧（`?limit=N&archived=1`） |
| `/api/workbench/session-timeline` | GET | タイムラインイベント（`?session_id=N&limit=N`） |
| `/api/workbench/session-meta` | GET/PUT/POST | title / tags / summary / archived の取得・更新 |
| `/api/workbench/session-summary` | POST | メッセージから title/summary/tags を自動生成 |
| `/api/workbench/session-export` | GET | ZIP エクスポート（`session.json` / `messages.md` / `raw/*`、`?redact=1` でシークレット除去） |
| `/api/workbench/state` | GET/POST/PUT | ワークスペース状態（選択セッション・タブ・比較対象） |
| `/api/workbench/templates` | GET/POST/DELETE | プロンプトテンプレート CRUD |
| `/api/workbench/palette` | GET/POST/DELETE | プロンプトパレット CRUD |
| `/api/workbench/tasks` | GET/POST/DELETE | タスクボード CRUD |
| `/api/workbench/policies` | GET/POST/DELETE | ポリシープロファイル CRUD |
| `/api/workbench/approval-simulate` | POST | provider + テキストから承認検出をシミュレート |
| `/api/workbench/diagnostics` | GET | git / claude / codex / copilot / cursor-agent / ollama のパス・バージョン確認 |
| `/api/workbench/files-context` | POST | 絶対パス群を読み prompt snippet 生成（1 ファイル 64KB / 合計 256KB 上限） |
| `/api/workbench/git-review` | GET | status / diff --stat / diff --name-status からリスク・コミット分割案を返す |
| `/api/workbench/worktrees` | GET/POST | git worktree 一覧（GET）/ 新規作成（POST） |
| `/api/workbench/file-watch` | GET | `git status --porcelain` ベースの変更ファイル一覧 |
| `/api/workbench/test-results` | GET | session store から FAIL/PASS/go test/npm test/pytest/vitest を全文検索 |
| `/api/workbench/redaction-preview` | POST | シークレットマスクプレビュー |
| `/api/workbench/usage` | GET | provider × model 別のメッセージ数・セッション数集計 |
| `/api/workbench/stale-sessions` | GET | 一定時間（既定 24h）更新のないセッション一覧（`?hours=N`） |
| `/api/session-chat` | GET | SQLite からチャット履歴を復元（`?session_id=N&limit=N`） |
| `/api/session-search` | GET | FTS5/LIKE で全セッション横断メッセージ検索（`?q=...`） |
| `/api/session-store/reset` | POST | 稼働中セッションを保護して履歴全リセット |

Workbench 系は `registerWorkbenchRoutes` で別途登録する。

### 11.3 Workbench UI（8 カード）

`web/src/app/workbench.ts`。ページ上部のセッションセレクター（ライブ + SQLite 保存済みをマージ）で対象を選び、以下 8 カードを操作する。

| カード | できること |
|---|---|
| Session replay / timeline | タイムライン表示、title/tags/summary 編集・自動要約、ZIP エクスポート |
| 同一プロンプト比較 | 同一プロンプトを複数ライブセッションへ同時送信、比較メモのタスク化 |
| Hub ワークスペース復元 | セッション選択・タブ・比較対象を JSON 保存・読み込み |
| Templates / Prompt palette | テンプレート・パレットの一覧・保存・入力欄投入 |
| Files からコンテキスト投入 | 絶対パス群から prompt snippet を生成し入力欄に追加 |
| Git review / watcher / worktree | 変更レビュー（リスク・分割案）、ファイル watcher、worktree 一覧・作成 |
| 安全 / 承認 / 診断 / 通知 | シークレットマスク、承認シミュレータ、provider 診断、通知権限確認、ポリシープロファイル |
| Task board / usage / stale cleanup | kanban タスクボード、利用量集計、放置セッション、テストログ検索 |

## 12. PWA + opt-in Web Push（v0.3.0 新規）

Hub UI を PWA 化し、オプトインで Web Push（W3C）通知を受け取れる。

### 12.1 構成ファイル

| 種別 | パス | 役割 |
|---|---|---|
| manifest | `web/src/manifest.webmanifest` | name / icons / `display: standalone` |
| service worker | `web/src/sw.ts`（配信は `dist/sw.js`） | push 受信・通知表示・notificationclick・Hub token キャッシュ |
| PWA フロント | `web/src/app/pwa.ts` | SW 登録 / `subscribeWebPush()` / `unsubscribeWebPush()` |
| アイコン | `web/src/icons/icon-192.png` / `icon-512.png` / `web/src/icon.svg` | manifest icons / 通知 icon・badge |
| サーバ | `internal/hub/push.go` | pushManager / VAPID 生成 / 購読 CRUD / sendApproval |
| 通知バックエンド | `internal/notify/notify.go` | ntfy / webhook への outbound 通知（Web Push とは別経路、§14） |

`web/web.go` の `//go:embed all:dist` により、SW / manifest / icons を含む `web/dist/` 全体がバイナリに同梱される（ビルド前にフロントエンドビルドが必須）。

### 12.2 VAPID 鍵と購読の保存

| 項目 | 値 |
|---|---|
| 保存先 | `~/.any-ai-cli/push_store.json`（perm `0o600`、tmp + atomic rename） |
| 鍵生成 | 起動時 `newPushManager()` 内で `webpush.GenerateVAPIDKeys()`。既存鍵があれば再利用 |
| ライブラリ | `github.com/SherClockHolmes/webpush-go` |
| 構造 | `vapid_public_key` / `vapid_private_key` / `subscriptions[]`（endpoint, keys{auth,p256dh}, user_agent, created_at, last_seen） |

`config.yaml` に VAPID 関連キーは持たせない（push_store.json で完結）。オプトイン状態のみ `user_prefs.push_notifications.enabled` に保存。

### 12.3 API

| Method | パス | 用途 |
|---|---|---|
| GET | `/api/push/status` | VAPID 公開鍵の有無・購読件数等のステータス |
| GET | `/api/push/vapid-public-key` | VAPID 公開鍵（SW 登録直前に取得） |
| POST | `/api/push/subscriptions` | 購読 upsert（`{endpoint, keys:{auth,p256dh}}`） |
| DELETE | `/api/push/subscriptions` | 購読解除 |

いずれも `s.guard()` で token 認証を通過後のみ処理。

### 12.4 通知挙動とセキュリティ

- 検出経路: PTY 出力処理で `approval_detected` を broadcast した直後に `notifyApprovalPush()` を goroutine 呼び出し。タスク完了は `notifyDonePush()` が同チャンネルを流用
- ペイロード: `type` / `id` / `session_id` / `provider` / `title` / `body`（最大 180 バイトに UTF-8 安全 truncate）/ `url`（`/?session_id=<id>`）
- TTL 300 秒、`pushManager.sent` map で 1 時間以内の再送を抑止
- **Hub token をペイロードに含めない**: `approvalPushURL()` は `token=` を一切付けず、`push_test.go` の `TestApprovalPushURLDoesNotIncludeToken` で回帰テスト。通知クリック時は SW が Cache Storage（`TOKEN_CACHE`、postMessage `any-ai-cli-token` 経由）からローカルに token を読み URL を構成するため、token が外部 push サーバ（FCM 等）に出ない
- 送信失敗ログは `endpointHash(endpoint)` のハッシュ先頭 12 文字のみ。SW の fetch handler は network-only（token-bound state をキャッシュしない）
- iOS Safari のブラウザタブ（PWA 未インストール）は `isLikelyIOSBrowserTabWithoutStandalone()` で検知し、登録前に「インストールが必要」と案内する

## 13. 統合 Windows ランチャー

WSL 専用の旧ランチャー `any-ai-cli-wsl.exe` は v0.3.0 で削除し、統合ランチャー `any-ai-cli-launcher.exe`（`cmd/any-ai-cli-launcher/main_windows.go`、非 Windows は `main_stub.go` でエラー終了）が WSL / SSH serve / SSH tunnel を一元管理する。Windows release zip は `any-ai-cli.exe` と `any-ai-cli-launcher.exe` の 2 本を同梱する。

### プロファイルファイル

保存先: `~/.any-ai-cli/launcher-profiles.yaml`（`config.yaml` とは分離）。

```yaml
version: 1
last_used: my-wsl
profiles:
  - name: my-wsl
    type: wsl
    distro: Ubuntu-22.04
    hub_port: 0             # 0 = Windows 側衝突を避けて自動選択
    cwd: /home/user/projects

  - name: my-vps
    type: ssh
    mode: serve             # serve（既定）= リモートで any-ai-cli serve
    host: vps.example.com
    user: your-user
    ssh_port: 22
    identity_file: ""
    hub_port: 47777
    cwd: /home/your-user/projects

  - name: vps-docker
    type: ssh
    mode: tunnel            # tunnel = 常駐 Hub へのポートフォワードのみ
    host: vps.example.com
    user: your-user
    hub_port: 47801         # 必須（0 不可）。リモート Hub と同ポート
    token_command: "docker exec aac-user1 sh -c 'grep ^token ~/.any-ai-cli/config.yaml | cut -d\" \" -f2'"
```

フィールド: `name` / `type`（wsl|ssh）/ `mode`（serve|tunnel、ssh のみ）/ `host` / `user` / `ssh_port` / `identity_file` / `hub_port` / `cwd` / `distro` / `binary` / `token_command`（tunnel のみ必須）。

### CLI フラグ

```
any-ai-cli-launcher.exe [--profile <name>] [--last] [--ui]
```

`--profile <name>`（指定で即接続）/ `--last`（前回プロファイル）/ `--ui`（常に選択画面）/ （なし: 1 件なら即接続、複数なら選択画面）。

### 接続フロー

**WSL プロファイル**（`internal/launcher/connector_wsl_windows.go` の `WSLConnector`）:

1. Windows 側で port を選ぶ（47777 から 100 刻みで listener を probe）
2. `wsl.exe [-d distro] --cd <cwd> -- bash -ilc 'export ANY_AI_CLI_WSL_LAUNCHER=1; exec any-ai-cli serve --port <port>'`
3. WSL 側 stdout/stderr を scan し banner の Hub URL を抽出
4. Windows の default browser を開く
5. 終了時に同 port の WSL orphan `serve` を best-effort cleanup

`bash -ilc` は Ubuntu 既定 `.bashrc` の非対話 early return を避け、pnpm / nvm / cargo の PATH を読むため。`ANY_AI_CLI_WSL_LAUNCHER=1` のとき Linux 側 Hub は log dir を Windows user profile 配下へ寄せる。

**SSH serve / tunnel**（`internal/launcher/connector_ssh_windows.go`）:

- serve: `ssh.exe -t -L <port>:127.0.0.1:<port> [user@]host -- bash -ilc "any-ai-cli serve --port <port>"` を起動し、banner の Hub URL を抽出。port 不一致なら kill して次候補で再試行（最大 5 回）。Ctrl+C でリモート serve を cleanup
- tunnel: `ssh.exe -N -L <port>:127.0.0.1:<port> [user@]host` でポートフォワードのみ確立 → `token_command` を SSH 経由実行（5 秒 timeout）→ `/api/info?token=<token>` を最大 20 回ポーリング（500ms 間隔）→ `/api/net-hint` に `ssh=true` / `host_label` / `env_kind=vps-tunnel` を登録 → ブラウザで `?token=...&via=ssh&host_label=...&env_kind=vps-tunnel` を開く。Ctrl+C でトンネルのみ閉じる（リモート Hub は停止しない）

tunnel モードのローカルポート衝突は `-o ExitOnForwardFailure=yes` で即失敗（+100 再試行はしない）。token は URL エンコードしてから付与する。

### セキュリティ前提

- Hub は `127.0.0.1` bind 固定。トンネルは `127.0.0.1` 同士の local forward のみ（`-g` / `GatewayPorts` 不使用）
- `token_command` の結果（token）は `launcher-profiles.yaml` に保存しない
- パスワード・パスフレーズは保存せず、`-o BatchMode=yes` で対話を禁止

実装: `internal/launcher/profile.go`（スキーマ / 読み書き / バリデーション）/ `connector_wsl_windows.go` / `connector_ssh_windows.go` / `cmd/any-ai-cli-launcher/main_windows.go`。

## 14. VPS / Docker デプロイ（v0.3.0 新規）

常駐リモート Hub を Docker コンテナで運用するための資産を `deploy/docker/` に追加。

| ファイル | 役割 |
|---|---|
| `deploy/docker/Dockerfile` | 4 ステージ（web-build → build → whisper-build → runtime） |
| `deploy/docker/Dockerfile.dockerignore` | BuildKit per-Dockerfile ignore |
| `deploy/docker/entrypoint.sh` | PID 1。Hub 起動・socat 中継・graceful shutdown |
| `deploy/docker/compose.yaml` | `include` で per-user YAML を束ねる |
| `deploy/docker/users/example.yaml` | per-user サービス定義テンプレート |
| `deploy/docker/aac-update.sh` | cron 用の冪等な自動更新スクリプト |
| `deploy/docker/sshd-member.conf.template` | メンバー SSH トンネル専用 sshd_config スニペット |
| `.github/workflows/docker-image.yml` | GHCR への image publish |

### Dockerfile

- `web-build`（node:22-bookworm + bun 1.3.14）: `web/dist` 生成
- `build`（golang:1.25-bookworm）: `CGO_ENABLED=0` 静的バイナリ（`-trimpath -ldflags "-s -w"`、`web/dist` は go:embed）
- `whisper-build`（ubuntu:24.04）: whisper.cpp をソースから可搬・静的ビルド（既定 `v1.8.6`、`BUILD_SHARED_LIBS=OFF` 等）
- runtime（ubuntu:24.04）: provider CLI（claude-code / codex / copilot / cursor-agent）をバージョン固定で導入し `DISABLE_AUTOUPDATER=1` で自己更新禁止。実行ユーザー ubuntu（uid 1000、sudo/adm 除外）、`EXPOSE 48000`、`ANY_AI_CLI_WHISPER_SERVER=/usr/local/bin/whisper-server`

HEALTHCHECK は Dockerfile ではなく per-user compose 側で定義。

### GHCR ワークフロー

- トリガー: `push`（main / develop）または `workflow_dispatch`
- タグ: main → `:latest`（VPS 通常運用が追従）、develop → `:develop`（検証）、全 push に `:sha-<short>`
- 認証: `docker/login-action` + `GITHUB_TOKEN`（`packages: write`）、キャッシュ `type=gha`、platforms `linux/amd64`

### compose（per-user）

`compose.yaml` は `include` で `users/<user>.yaml` を取り込む。ユーザー追加 = YAML 1 ファイル + include 1 行。`users/example.yaml` の要点:

| 設定 | 値 / 方針 |
|---|---|
| image | `ghcr.io/ishizakahiroshi/any-ai-cli:${AAC_TAG:-latest}` |
| restart / stop_grace_period | `unless-stopped` / 40 秒 |
| ログ | json-file, max-size 10m, max-file 3 |
| security_opt | `no-new-privileges:true` |
| ports | `127.0.0.1:<USER_PORT>:48000`（ホスト側 loopback のみ） |
| volumes | named volume `aac-home-<user>` + work bind mount |
| networks | ユーザー専用 `aac-net-<user>`（コンテナ間 Hub 直アクセス遮断） |
| 制限 | mem/memswap 各 2g、cpus 2、pids_limit 1024 |
| healthcheck | `curl http://127.0.0.1:$HUB_PORT/api/info?token=<token>`（interval 60s / timeout 5s / retries 3 / start_period 15s） |

### aac-update.sh

`/opt/any-ai-cli/aac-update.sh`。cron 例 `30 4 * * * ...`（毎日 04:30）。同ディレクトリに `HOLD` ファイルがあればスキップ（idempotent、解除は `rm HOLD`）。フロー: `docker compose pull --quiet` → `docker compose up -d`（イメージ変化時のみ再作成）→ `docker image prune -f`。追従タグは `.env` の `AAC_TAG` で切替。

### localhost-only の維持

- compose の publish は必ず `127.0.0.1:<PORT>:48000`。コンテナ内の `0.0.0.0` bind は socat 中継（`TCP-LISTEN:48000 → 127.0.0.1:$HUB_PORT`）の Namespace 内に閉じる
- Hub は `HUB_PORT` 完全一致を要求（`isAllowedHubHost`）。entrypoint は `serve --port $HUB_PORT` で publish ポートと三者一致
- ユーザー間は専用 Docker network で分離。SSH は `sshd-member.conf.template` で `PermitOpen 127.0.0.1:__PORT__` / `ForceCommand nologin` / `PermitTTY no` / `GatewayPorts no` 等によりトンネル以外を遮断

## 15. Voice / Whisper

音声入力は WebSpeech API に加え、ローカル Whisper サーバ連携を `config.yaml:voice.whisper` で設定できる（`web/src/app/voice-whisper.ts` / `voice.ts`、`/api/voice/transcribe`、`/api/whisper/*`）。Docker runtime は whisper-server を同梱し、`ANY_AI_CLI_WHISPER_SERVER` で位置を Hub に通知する。wake word 関連 key は schema に残るが公開 UI では hidden / disabled。

| API | Method | 用途 |
|---|---|---|
| `/api/voice/transcribe` | POST | 音声文字起こし |
| `/api/whisper/status` | GET | Whisper サーバ状態 |
| `/api/whisper/install` / `uninstall` | POST | モデル導入 / 削除 |
| `/api/whisper/start` / `stop` | POST | managed サーバ起動 / 停止 |

## 16. Outbound 通知（ntfy / webhook）

Web Push とは別経路の outbound 通知を `config.yaml:notify` で設定する（`internal/notify/notify.go`）。承認待ち / タスク完了を ntfy トピックや webhook へ送る。

| API | Method | 用途 |
|---|---|---|
| `/api/notify-config` | GET/POST | 通知バックエンド設定 |
| `/api/notify-test` | POST | テスト通知送信 |
| `/api/notify-generate-topic` | POST | ntfy トピック生成 |

設定例は §18 の `notify:` を参照。`events: []`（空）の場合は送信しない（オプトイン）。

## 17. Path / open handlers・Attach・Logging

### Path / open handlers

Terminal / Chat / Files から local path を扱う API: `/api/open-file` / `/api/open-default-file` / `/api/open-folder` / `/api/open-terminal` / `/api/file-open-app` / `/api/terminal-app` / `/api/pick-file` / `/api/pick-directory` / `/api/path-exists` / `/api/open-dir`。Windows は explorer / configured app、WSL launcher mode は `wslpath -w` 経由で Windows path に変換してから開く。localhost-only 前提の host operation API で、loopback remote address を確認する。

### Attach

画像に加え generic file attach を受け付ける。paste / D&D / file picker から `pendingAttachFiles` に積み、送信時に `/api/attach` へ multipart POST。保存は `~/.any-ai-cli/attachments/<session-id>/`（timestamp + nanosecond + sanitized name）。inject は Claude `@<path>` / Codex `@<slash path>` / fallback `<path>`。画像は可能なら長辺 1568px 以下の JPEG へ normalize。添付の保持は `log.attachment_retention_days` / `attachment_max_total_mb` と `/api/attachments/purge` で管理。

### Logging / Transcript

| 種類 | path |
|---|---|
| Hub log | `~/.any-ai-cli/logs/hub.log` |
| spawn log | `~/.any-ai-cli/logs/spawn/<provider>-<timestamp>.log` |
| session raw log | `~/.any-ai-cli/logs/sessions/*.log` |
| session history | `~/.any-ai-cli/logs/sessions/*.jsonl` |
| clean transcript | `~/.any-ai-cli/logs/sessions/*.txt` |
| SQLite store | `~/.any-ai-cli/logs/any-ai-cli.db`（§11.1） |

Clean transcript は session end 時に自動生成し、crash で漏れた分は次回 `serve` 起動時に `recoverTranscripts` が補完する。手動生成は `any-ai-cli log-clean <session>.jsonl -o transcript.txt`。session log は v0.2.0 以降オプトイン（`log.session_enabled`、既定 false）で、保持は `log.session_retention_days` / `session_max_size_mb` と `/api/logs/purge`・`/api/logs/legacy-notice` で管理する。

## 18. CLI / サブコマンド

`cmd/any-ai-cli/main.go` のサブコマンド:

| サブコマンド | 引数 / フラグ | 用途 |
|---|---|---|
| （なし） | — | Hub 起動済みならブラウザを開く。未起動なら serve + auto-open |
| `serve` | `--open` / `--port <n>` / `--dev` / `--debug` | Hub HTTP サーバ起動 |
| `wrap <provider>` | provider + 任意引数 | 指定 provider を Hub 経由ラッパー起動 |
| `claude` / `codex` / `copilot` / `cursor-agent` | 任意引数 | `wrap <provider>` の糖衣構文 |
| `shell-init` | — | シェル統合スクリプト出力（eval 用） |
| `status` / `stop` | — | Hub 稼働状況表示 / 停止 |
| `log-clean` | `<session.jsonl>` `[-o transcript.txt]` | JSONL → テキスト transcript 変換 |
| `uninstall` | `[--purge]` | 設定削除（`--purge` でバイナリ本体も） |
| `version` / `--version` / `-v` | — | バージョン表示（ldflags → git tag の順） |
| `usage-relay` | （内部） | Claude statusLine / Codex Stop フックからの隠しリレー（help 非表示） |

バイナリ構成: `cmd/any-ai-cli/`（全 OS、メイン CLI）/ `cmd/any-ai-cli-launcher/`（Windows、統合ランチャー）。旧 `cmd/any-ai-cli-wsl/` は削除済み。

## 19. HTTP API 一覧（カテゴリ別・静的アセット除く）

v0.3.0 時点で約 93 エンドポイント。主要カテゴリ:

- **セッション管理**: `/api/info` `/api/net-hint` `/api/spawn` `/api/kill-all` `/api/shutdown` `/api/idle-timeout` `/api/reconnect-grace` `/api/session-usage`
- **WebSocket**: `/ws`（UI 接続 / wrapper register / reattach）
- **セッション履歴**: `/api/session-chat` `/api/session-log` `/api/session-search` `/api/session-store/reset`
- **承認**: `/api/approval/status|enable|disable|dismiss` `/api/approval-patterns` `/api/approval-patterns/`
- **Files**: `/api/files-list|content|asset|download|roots|move|rename|mkdir|create|save|delete-dir`
- **Git**: `/api/git-log|show|refs|status|commit-all|commit-message|fetch|pull`
- **モデル / slash**: `/api/models` `/api/slash-cmd-sources` `/api/slash-commands` `/api/usage-link-defaults`
- **設定 / 通知**: `/api/log-config` `/api/notify-config` `/api/notify-test` `/api/notify-generate-topic` `/api/encoding-check`
- **user-prefs**: `/api/user-prefs` `/api/user-prefs/notify-sound-custom` `/api/user-prefs/avatar` `/api/avatar`
- **Push**: `/api/push/status` `/api/push/vapid-public-key` `/api/push/subscriptions`
- **Voice / Whisper**: `/api/voice/transcribe` `/api/whisper/status|install|uninstall|start|stop`
- **Path / open**: `/api/attach` `/api/pick-directory` `/api/pick-file` `/api/path-exists` `/api/open-file|default-file|folder|terminal|dir` `/api/file-open-app` `/api/terminal-app`
- **ログ保守**: `/api/logs/purge` `/api/logs/legacy-notice` `/api/attachments/purge`
- **Workbench**: `/api/workbench/*`（§11.2 参照）

旧 docs browser 系 `/api/docs-*` は `/api/files-*` へ移行済み（browser storage も one-shot migration）。

## 20. 設定スキーマ

`~/.any-ai-cli/config.yaml` の主要 schema（v0.2.0 §16 からの追加を含む）:

```yaml
hub:
  port: 47777
  open_browser: true
  auto_shutdown: true
  log_dir: ~/.any-ai-cli/logs
  idle_timeout_min: 60
  wrapper_reconnect_grace_sec: 3600
  allow_loopback_without_token: false
  trusted_networks: []
  allowed_hosts: []
  env_kind: ""           # local | wsl | vps | vps-tunnel

log:
  enabled: true
  max_size_mb: 10
  max_backups: 3
  compress: false
  session_enabled: false              # v0.3.0: PTY セッションログのオプトイン
  legacy_logs_notice_shown: false     # v0.3.0
  session_retention_days: 7           # v0.3.0
  session_max_size_mb: 50             # v0.3.0
  attachment_retention_days: 7        # v0.3.0
  attachment_max_total_mb: 500        # v0.3.0

approval:
  enabled: false
  first_launch_shown: false

models_source: ""        # v0.3.0: /api/models のデフォルト取得元 URL 上書き

voice:                   # v0.3.0
  whisper:
    server_url: ""
    request_path: ""
    language: ja
    timeout_seconds: 60
    managed: false
    model: small
    server_port: 0
    hallucination_phrases: []   # 既定 10 件

notify:                  # v0.3.0: outbound 通知（Web Push とは別経路）
  backends:
    - type: ntfy         # ntfy | webhook
      url: ""
      topic: ""
  events: []             # approval | done（空 = 送信しない）

slash_cmd_sources:
  claude: https://raw.githubusercontent.com/ishizakahiroshi/any-ai-cli/main/resources/slash-commands/claude.md
  codex: ...
  copilot: ...
  cursor-agent: ...

approval_pattern_sources:
  claude: .../resources/approval-patterns/claude.md
  codex: ...
  copilot: ...
  cursor-agent: ...
  common: .../resources/approval-patterns/common.md

approval_profiles:
  claude: official
  codex: official
  copilot: official
  cursor-agent: official
  common: official

local_models:
  - id: llama3.2:3b
    label: Llama 3.2

file_open_app: ""
terminal_app: ""
token: <32-byte-hex>

user_prefs:
  trigger: { enabled: false, phrase: "" }
  notify_sound: { enabled: false, type: default, custom_file: "", custom_mime: "" }
  approval: { auto_switch: false }
  quick_cmds: { cmd1: "", cmd2: "" }
  usage_links: { claude: "", codex: "", copilot: "", cursor-agent: "" }
  voice: { grace_seconds: 2, wake_word_enabled: false, wake_word_phrase: "", input_disabled: false }  # input_disabled は v0.3.0
  desktop_notifications: { enabled: false }   # v0.3.0
  push_notifications: { enabled: false }      # v0.3.0
  display: { theme: "", font_size: "", lang: "", locked_mode: "" }  # v0.3.0
  token_statusbar: { enabled: null }          # v0.3.0（*bool。null = 既定 ON）
  done_summary_notify: { enabled: false }     # v0.3.0
  favorites: []
  session_order: []
  group_order: []
  project_favorites: []
  cwd_history: []
  spawn: { defaults: {}, last_model: {} }
  avatar: ""
  display_name: ""
  migrated_from_localstorage: false
```

VAPID 鍵・Web Push 購読は `config.yaml` ではなく `~/.any-ai-cli/push_store.json` に保存する（§12.2）。launcher プロファイルは `~/.any-ai-cli/launcher-profiles.yaml`（§13）。Workbench の templates / tasks / palette / policies / state は `~/.any-ai-cli/workbench/*.json`、セッション履歴は `~/.any-ai-cli/logs/any-ai-cli.db`。

## 21. Security / Privacy

v0.3.0 は遠隔運用と通知が増えたため、localhost-only 前提とトークン分離をより重視する。

守る前提:

- Hub は `127.0.0.1` bind 固定。Docker は loopback publish + socat 中継、リモートは SSH local forward に限定
- token なし API / WebSocket は既定で拒否。例外は `hub.allow_loopback_without_token: true` の場合のみで、RemoteAddr が loopback または `hub.trusted_networks` の CIDR に入るリクエストを許可（`0.0.0.0/0` / `::/0` は設定エラー）
- `hub.allowed_hosts` は WireGuard 等の私設経路用の Host / Origin 許可リスト（IP literal / hostname のみ、wildcard / port 付きは設定エラー）
- POST 系は token + メソッド + Host/Origin チェックを通す。`/api/open-*` 等は loopback remote address を確認
- **Web Push 通知に Hub token を含めない**（§12.4）。push_store.json は perm 0o600、VAPID 秘密鍵を保護
- launcher は token / パスワード / パスフレーズを保存しない（§13）
- session raw log / jsonl / transcript / SQLite store / attach は機密情報として扱う
- reverse proxy / `0.0.0.0` bind / 認証なしのリモート公開は禁止

Outbound traffic:

- slash command sources / approval pattern sources / model defaults: GitHub raw
- Web Push: ブラウザが購読した push サービス（FCM 等）。token は送らない
- outbound 通知（任意設定時）: ユーザー設定の ntfy / webhook
- wrapped CLI traffic: Claude / Codex / Ollama 側の通常通信

`any-ai-cli` 自身は telemetry を送信しない。

## 22. 既知の注意点・保留

- `/api/files-delete-dir` は「空ディレクトリ削除」を意図する API だが、実装が `os.RemoveAll` を用いるため非空でも削除され得る点に注意（UI からは空 dir 削除導線のみ）。設計意図と実装の整合は要確認
- Wake word は実装痕跡があるが公開 UI では hidden / disabled
- Chat history は in-memory が主。SQLite からの復元は `/api/session-chat` で可能だが、完全な jsonl backfill は公開仕様外
- native Linux / macOS は build 対象だが十分な実機検証は未完了
- OpenCode は UI / config / usage-links に痕跡が残るが spawn 非対応（非公開）
- Cursor Agent の native approval パターンは実機での網羅検証が継続課題

## 23. 関連資料

- `CHANGELOG.md`（`[0.3.0] - 2026-06-05`）
- `README.md` / `README.ja.md`
- `docs/v0.2.0-any-ai-cli-design.md`（前版設計書）
- `docs/manual_ollama-cloud-routing.md`
- `deploy/docker/`（Dockerfile / compose / aac-update.sh / sshd-member.conf.template）
- `.github/workflows/docker-image.yml`
- `internal/sessionstore/store.go` / `internal/hub/workbench_handlers.go` / `internal/hub/push.go` / `internal/launcher/`
