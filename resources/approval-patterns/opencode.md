# OpenCode Approval Patterns

> many-ai-cli の Hub UI が OpenCode CLI の permission UI を検出するためのトリガー文言。
> 1 行 1 パターン。バッククオートで囲んだ部分のみをパースする。
> 実機 UI（Build agent 承認ダイアログ）の文言を正とする。

- `permission required`
- `allow once`
- `allow always`
- `reject`
