# Command Code Approval Patterns

> many-ai-cli の Hub UI が Command Code CLI の permission UI を検出するためのトリガー文言。
> 1 行 1 パターン。バッククオートで囲んだ部分のみをパースする。

実機 PTY の採取（親 plan C6）が済むまで trigger を置かない。空のファイルは検出しないことを意味する。
