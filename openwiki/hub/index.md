# Files

- [Approval Detection and Marker System](approval-detection.md) - How the Hub detects an AI CLI's approval prompt across providers — transcript vs terminal-mirror marker sources, the single candidateKey+sourceEpoch identity rule, pattern-profile trigger phrases, and the separate opt-in auto-approval policy layer.
- [Files and Git Tabs](files-and-git.md) - The Hub's file-browsing/editing HTTP endpoints — their read-scope trust model, atomic no-clobber renames, and secure on-disk permissions — plus the Git tab's argv-only git invocation.
- [Notifications and Done Summaries](notifications.md) - How the Hub always surfaces completion/approval state in-UI, and how the two separate opt-in external channels — Web Push and outbound ntfy/webhook — deliver it without leaking the Hub auth token.
- [Light Orchestration and the Relay Loop](orchestration-relay.md) - How a conductor session spawns child AI sessions with a shared board.md, and how the Hub-driven relay state machine runs a plan through implementation, adversarial review, and fix without an AI conductor in the loop.
- [Security, Auth, and Remote Access Protection](security-remote-access.md) - The Hub's token-based auth model, its loopback-only binding, the kill-switch revoke-all endpoint, the optional PIN gate for non-loopback access, and new-device connection notifications.
- [Hub HTTP/WebSocket Server and Session Registry](server-and-sessions.md) - The Hub's session record and three-axis activity model, the single flat proto.Message struct used for both WebSocket directions, the categorized HTTP API surface, and the allowlisted bug-report collector.
- [Session Logging, Transcripts, and Attach](session-logging-and-attach.md) - The Hub's structured log file, the opt-in raw PTY session log with secret masking, clean transcript generation, the SQLite session-history store, and the file/image attachment store used by paste-and-drop.
- [Multiple Subscriptions Per Provider](subscriptions.md) - How many-ai-cli isolates several logins for one provider CLI purely through per-profile config-directory environment variables, never touching credentials itself, plus how remaining-quota usage figures are read.
- [Voice Input and Managed Whisper](voice-input.md) - Browser-recognition vs. local-Whisper voice dictation, the mobile AudioWorklet-to-Hub relay path, false-trigger suppression, and the self-contained managed Whisper binary/model lifecycle (Windows-only).
