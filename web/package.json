{
  "name": "any-ai-cli-web",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "tsc --noEmit",
    "watch": "node scripts/build.mjs --watch",
    "test": "npm run build && node --test dist/app/approval-parser-fixtures.js"
  },
  "type": "module",
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-unicode11": "^0.9.0",
    "@xterm/addon-web-links": "^0.12.0",
    "@xterm/addon-webgl": "^0.19.0",
    "@xterm/xterm": "^6.0.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  }
}
